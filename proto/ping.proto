syntax = "proto3";
package ping;

service Ping {
  rpc Ping (PingRequest) returns (PingReply);
}

message PingRequest {
  string version = 1;
}

message PingReply {
  bool ready = 1;
  string version = 2;
  uint64 block_height = 3;
}